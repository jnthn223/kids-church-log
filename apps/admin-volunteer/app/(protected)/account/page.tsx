"use client";
import { Card, Button, StatusChip } from "@kcl/ui";
import { useAuthAccess } from "@kcl/firebase";
export default function AccountPage() { const { member, user, signOutUser } = useAuthAccess(); return <><div className="page-heading"><div><h2>Account & help</h2><p>Your individual ministry identity, registration guidance, and privacy reminders.</p></div></div><div className="grid grid-2"><Card className="section" style={{ marginTop: 0 }}><div className="section-head"><h3>Signed-in account</h3><StatusChip tone="success">Active</StatusChip></div><dl><div className="definition"><dt>Name</dt><dd>{member?.displayName}</dd></div><div className="definition"><dt>Email</dt><dd>{user?.email}</dd></div><div className="definition"><dt>Role</dt><dd>Admin Volunteer</dd></div></dl><Button variant="secondary" onClick={() => { localStorage.removeItem("kcl-admin-registration-draft"); void signOutUser(); }}>Sign out</Button></Card><Card className="section" style={{ marginTop: 0 }}><h3>Registration reminders</h3><ul className="muted"><li>Search before creating a family.</li><li>Read back safety information before saving.</li><li>Never treat possession of a pass as authority to edit a record.</li><li>Escalate disputed pickup access, deactivation, or administrative pass disablement to a Ministry Lead.</li></ul><p className="help">KidsChurchLog Admin Volunteer · version 0.1.0</p></Card></div></>; }
