"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { BrandMark, Button } from "@kcl/ui";
import { createEmailAccessAccount, submitAccessRequestDetails } from "@kcl/firebase";
export function AccessRequestForm({ recovery = false }: { recovery?: boolean }) { const router = useRouter(); const [values, setValues] = useState({ displayName: "", email: "", password: "", confirm: "", ministryResponsibility: "Family registration", requestReason: "" }); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const set = (key: keyof typeof values, value: string) => setValues((current) => ({ ...current, [key]: value })); async function submit(event: FormEvent) { event.preventDefault(); if (!recovery && values.password !== values.confirm) return setError("Passwords do not match."); setBusy(true); setError(""); try { const details = { displayName: values.displayName, ministryResponsibility: values.ministryResponsibility, requestReason: values.requestReason }; if (recovery) await submitAccessRequestDetails(details, "ADMIN_VOLUNTEER"); else await createEmailAccessAccount({ ...details, email: values.email, password: values.password }, "ADMIN_VOLUNTEER"); router.replace("/access/"); } catch (value) { setError(value instanceof Error ? value.message : "Your request could not be submitted."); } finally { setBusy(false); } } return <main className="auth-page"><section className="auth-story"><BrandMark /><div><span className="auth-tag">Registration team access</span><h1>Help families feel known from their first visit.</h1><p>Accounts remain pending until a Ministry Lead approves the Admin Volunteer role.</p></div></section><section className="auth-panel"><form className="auth-form" onSubmit={submit}><BrandMark /><h2>{recovery ? "Complete your request" : "Request access"}</h2><p>Use your own ministry account. Family members never need an account.</p>{error && <div className="form-error" role="alert">{error}</div>}<div className="field"><label htmlFor="name">Full name</label><input id="name" required minLength={2} value={values.displayName} onChange={(e) => set("displayName", e.target.value)} /></div>{!recovery && <><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" required value={values.email} onChange={(e) => set("email", e.target.value)} /></div><div className="field"><label htmlFor="password">Password</label><input id="password" type="password" required minLength={12} value={values.password} onChange={(e) => set("password", e.target.value)} /><small className="help">Use 12–128 characters and avoid common passwords.</small></div><div className="field"><label htmlFor="confirm">Confirm password</label><input id="confirm" type="password" required value={values.confirm} onChange={(e) => set("confirm", e.target.value)} /></div></>}<div className="field"><label htmlFor="responsibility">Ministry responsibility</label><input id="responsibility" required minLength={2} maxLength={120} value={values.ministryResponsibility} onChange={(e) => set("ministryResponsibility", e.target.value)} /></div><div className="field"><label htmlFor="reason">Why do you need access?</label><textarea id="reason" required minLength={10} maxLength={500} value={values.requestReason} onChange={(e) => set("requestReason", e.target.value)} /></div><Button disabled={busy}>{busy ? "Submitting…" : "Submit request"}</Button></form></section></main>; }
